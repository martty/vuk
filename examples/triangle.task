#version 450
#extension GL_EXT_mesh_shader : require
#pragma shader_stage(task)

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    // Dispatch 1 mesh shader workgroup
    EmitMeshTasksEXT(1, 1, 1);
}
